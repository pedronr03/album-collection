type Song {
  id: Int
  title: String
  length: Int
  album: Album
}

input CreateSongInput {
  title: String!
  length: Int!
  albumId: Int!
}

input UpdateSongInput {
  id: Int!
  title: String
  length: Int
  albumId: Int
}